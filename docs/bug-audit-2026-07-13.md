# 一般ユーザー本線フロー バグ監査報告書

- 調査日: 2026-07-13
- 対象: kanjiname.jp 本線フロー（ランディング → 決済 → 質問回答 → AI生成 → 結果表示）
- 手法: 机上調査のみ（読み取り専用。実行・外部接続・修正は一切なし）
- 受益者: 診断を購入する一般エンドユーザー（11言語圏・非日本語話者中心）
- 凡例: `[事実]` = Read/Grep/機械照合で確認済み（file:line 付き）、`[推測]` = コードから導いた未実測の帰結

前提となるデプロイ構成の確認:
<known-fact>ルート vercel.json の buildCommand が frontend をビルドして public/ へ配置し、API はリポジトリ直下 `api/` を使用する（vercel.json buildCommand / rewrites）。`frontend/api/` は旧構成の残骸と判断（デプロイ実体は未確認）。</known-fact>

---

## 1. バグ一覧

### 影響度: 高

<finding id="H-1" severity="high" title="ポルトガル語(pt)ユーザーは決済後、質問文・選択肢がすべて空白になり進行不能">
<known-fact>
- frontend/src/questions.json: 全17問（Q0〜Q16）の text および全選択肢の text に `pt` キーが存在しない（node による機械照合で 100% 欠落を確認。Q1 の text キーは ja,en,fr,de,es,it,th,vi,id,ko のみ）。
- frontend/src/App.js:152 `{question.text[language]}`、App.js:162 `{option.text[language]}` — フォールバック処理なし。language='pt' なら undefined を描画。
- frontend/src/contexts/LanguageContext.js:4 で `pt` は SUPPORTED_LANGUAGES に含まれ、言語セレクタから選択可能。
- frontend/src/locales/index.js は pt の UI キーを完備（62キー全揃い）→ ランディング・決済は pt で正常表示され、支払いまで到達できてしまう。
- src/services/AIKanjiGenerationService.ts:56-67 の LANGUAGE_CONFIG にも `pt` が欠落（en フォールバック）。
</known-fact>
<risk>再現条件: `?lang=pt`（またはブラウザ言語 pt）でアクセス → 決済 → 名前入力 → 質問画面。質問文と選択肢が空白のカードが表示され、ユーザーは支払済みのまま実質進行不能（ボタンだけは押せるが何を選んでいるか分からない）。ポルトガル語圏ユーザー全員に発生。</risk>
</finding>

<finding id="H-2" severity="high" title="決済後にリロード/タブ閉じ/ブラウザバックすると全状態が消失し、復帰手段が再決済しかない">
<known-fact>
- App.js:658-663 hasPaid / paymentIntentId / sessionId / result はすべて React state（メモリのみ）。永続化されるのは partnerCode（sessionStorage, App.js:33）と言語設定のみ。
- 結果復元用 API `GET /api/generate?session_id=xxx&action=result` は実装済み（api/generate.ts:40-53）だがフロントから一切呼ばれていない（App.js に該当呼び出しなし）。
- ApiClient.getNextQuestion（App.js:78-83）も未使用でセッション再開経路が存在しない。
</known-fact>
<risk>再現条件: 決済完了後の任意の時点（質問回答中・生成中・結果表示後）でページをリロード → ランディングページに戻り、支払済みであることをアプリは認識しない。結果表示後でも、書道申込（メール送信）をしていなければ結果を取り戻す手段がない。ユーザーの自然な回復行動（リロード）が「もう一度支払う」に直結する二重課金誘発設計。</risk>
</finding>

<finding id="H-3" severity="high" title="セッション作成のHTTPエラーが黙殺され、全回答が静かに送信されず、最後に英語のデッドエンドに落ちる">
<known-fact>
- App.js:69-76 ApiClient.createSession は `response.ok` を確認せず `data.session_id` を返す。API が 500 を返しても JSON パースは成功するため catch（App.js:772-775）は発火せず、sessionIdRef.current = undefined のまま何のエラー表示もない。
- App.js:826 `if (sessionIdRef.current && currentQuestionId !== 'Q0')` — sessionIdRef が falsy の間の回答は送信もリトライキュー追加もされず黙って破棄される。
- App.js:845-855 最終質問後に 5 秒だけ待機し、`setError('Session not ready. Please try again.')`（英語ハードコード・リトライ手段なし、エラー画面は App.js:1150-1152 の `<div className="error">` のみ）。
- 具体的な再現例: sessions.user_name は VARCHAR(100)（schema.sql:9）だが名前入力欄に maxLength がない（App.js:1098-1105）→ 101文字以上の入力で INSERT が失敗し 500 → 上記の無音経路に入る。
</known-fact>
<risk>再現条件: (a) 100文字超の名前入力、(b) DB障害・コールドスタート中のセッション作成失敗、(c) セッション作成が遅い間に素早く回答を進める（この場合は初期回答のみ欠落し H-4 の INSUFFICIENT_ANSWERS に接続）。いずれも決済済みユーザーが 17 画面回答した後に英語のデッドエンドまたは白画面。</risk>
</finding>

<finding id="H-4" severity="high" title="生成APIのエラー応答(422/500)をそのまま結果として扱い、白画面（無反応画面）になる">
<known-fact>
- App.js:98-104 ApiClient.generateKanjiName は `response.ok` を確認せず `response.json()` を返す。api/generate.ts:62-79 はエラー時 `{error:{code,message}}` を返すため、これがそのまま `setResult()` される（App.js:877-878）。
- App.js:482-484 ResultCard は `if (!result || !result.explanation) return null;` → 結果画面の分岐（App.js:1155）には入るが中身が null。ユーザーには言語セレクタとフッターだけの画面が表示される。console 以外に手掛かりなし。
- 到達経路の例: 回答が16件に満たない場合の INSUFFICIENT_ANSWERS 422（GenerationService.ts:43-47。H-3(c)の回答欠落で発生）、GEMINI_API_KEY 不備（AIKanjiGenerationService.ts:73-76 で throw → 500）、Gemini 3回連続失敗（同 :91-127）。
- 同種の問題: ApiClient.submitAnswer（App.js:85-96）も ok 未確認のため、HTTP 4xx/5xx の回答送信失敗が「成功」として扱われ（App.js:827-833 の catch はネットワーク断でしか発火しない）、リトライ機構（App.js:870-874）の対象にもならない。
</known-fact>
<risk>再現条件: 生成APIがエラーJSONを返す状況全般。決済済みユーザーがローディング終了後に完全な白画面で放置される。サイレント失敗の中で最も重大。</risk>
</finding>

<finding id="H-5" severity="high" title="AI生成の失敗・タイムアウト時にリカバリ導線がなく、リロード→再決済（二重課金）に誘導される構造">
<known-fact>
- api/generate.ts に maxDuration 設定がない。一方 api/submit-email.ts:24 には `export const maxDuration = 60; // Vercel Pro: allow up to 60s for generation` がある（ただし submit-email はフロントから未使用の旧フロー残骸 — frontend/src 内に呼び出しなしを grep で確認）。
- AIKanjiGenerationService.ts:91-127 のリトライは 503 時に 5 秒・10 秒待機して最大 3 回試行 → 待機だけで 15 秒。関数の実効上限がプランデフォルト（例: 15秒）の場合、最終試行に到達できない。
- タイムアウト時、Vercel は非JSONの 504 を返す → App.js:877 `response.json()` が throw → App.js:880 `setError('Failed to generate result')`（英語ハードコード）→ App.js:1150-1152 のエラー画面。再試行ボタンなし。
</known-fact>
<unconfirmed>本番の実効タイムアウト値（プラン・Fluid Compute の有無）は未確認。maxDuration の欠落と submit-email.ts との設定非対称は事実だが、実際に 504 が発生する頻度は実測しないと断定できない。</unconfirmed>
<risk>生成失敗の種類を問わず、ユーザーに提示される回復手段はゼロ。自然な行動（リロード）は H-2 により再決済画面に着地する。「エラー画面 + 支払済み + 復帰不能」の組合せは返金請求・チャージバックの直接原因になる。</risk>
</finding>

### 影響度: 中

<finding id="M-1" severity="medium" title="「戻る」で回答を修正しても、サーバー側は最初の回答のまま生成される">
<known-fact>
- schema.sql:25 answers に UNIQUE(session_id, question_id)。
- src/services/AnswerService.ts:33-40 既回答なら upsert せず ALREADY_ANSWERED を throw → api/answers.ts:55-62 が 409 を返す。
- App.js:827-833 フロントは response.ok を見ないため 409 を成功扱い。回答修正はサーバーに一切反映されない。
- App.js:779-802 handleBack はローカル履歴を戻すだけでサーバー状態に触れない。
</known-fact>
<risk>再現条件: 任意の質問に回答 → 「戻る」→ 別の選択肢を選んで進む。UI 上は修正できたように見えるが、AI 生成（性格プロファイル）は修正前の回答で行われる。ユーザーは「自分の答えと違う診断」を受け取るが、それに気づく手段がない。</risk>
</finding>

<finding id="M-2" severity="medium" title="結果の説明文が9言語で英語のまま表示される（lang パラメータの三重の契約不一致）">
<known-fact>
- (a) フロントは `POST /api/generate?...&lang=${language}` で言語を送る（App.js:99）が、api/generate.ts:35 は `generationService.generateKanjiName(session_id as string)` と lang を渡さない。GenerationService.ts:25 のデフォルト 'en' が常に使われ、ユーザー言語版の説明文（explanation_user / meaning_user）はそもそも生成されない（AIKanjiGenerationService.ts:209-221 の needsUserLang 分岐が活きない）。
- (b) 仮に渡っても、サーバーの応答キーは `explanation.user` / `meaning_user`（GenerationService.ts:226-244）だが、フロントは `explanation[language]`（App.js:496）と `meaning_${language}`（App.js:488）を参照する。キー体系が食い違っており永遠にヒットしない。
- (c) AIKanjiGenerationService.ts:56-67 LANGUAGE_CONFIG に pt 欠落（H-1 と同根）。
- 結果: fr/de/es/pt/it/th/vi/id/ko のユーザーは App.js:501 のフォールバックで英語説明文を見る。
</known-fact>
<risk>「11言語対応」を掲げた有料商品の中核成果物（説明文）が9言語で英語のまま。返金理由・低評価レビューに直結する。機能欠陥だが進行は可能なため中とした。</risk>
</finding>

<finding id="M-3" severity="medium" title="決済UI（PaymentModal）が i18n 非対応 — 英語と日本語のハードコード混在">
<known-fact>
- 英語固定: 'Preparing payment...'（PaymentModal.js:257,323）、'Pay $X & Start' / 'Processing...'（:86）、'Payment was not completed. Please try again.'（:50）、'Complete Your Purchase'（:307）、'Retry'（:265）。
- 日本語固定: 'プロモコードをお持ちの方' / '閉じる'（:286,351）、'デモコードが適用されました！'（:250,315）、DemoCodeInput のエラー文言（:102,124,127）。
- PaymentModal.js は useTranslation を import していない（全文確認）。Stripe Elements にも locale 指定なし（:232-241 の options に locale なし → ブラウザ言語依存）。
</known-fact>
<risk>コンバージョンの最重要地点である決済フォームで、タイ語・韓国語等のユーザーに英日混在 UI が出る。エラー時の文言（カード拒否等）も英語のみで、決済離脱・誤操作の温床。</risk>
</finding>

<finding id="M-4" severity="medium" title="決済とセッション/結果がDB上で永遠に紐付かない + calligrapher-request が payment_intent_id を破棄">
<known-fact>
- PaymentIntent はランディング表示時（セッション作成前）に生成され、PaymentModal.js:196-200 のリクエストボディに session_id が含まれない → api/stripe/create-payment-intent.js:63 で metadata.session_id は常に '' → api/stripe/webhook.js:131 で payments.session_id は常に NULL。
- フロントは calligrapher-request に payment_intent_id / partner_code を送る（App.js:312-313）が、api/calligrapher-request.js:34 は `{ email, kanji_name, user_name, language, explanation_ja, explanation_user_lang }` しか受け取らず、INSERT（:58-74）にも含めない。両フィールドは黙って破棄される。
</known-fact>
<risk>「この支払いはどのセッション・どの結果・どの書道申込に対応するか」を機械的に照合できない。二重課金の疑い・返金請求・書道未着問い合わせの際、メールアドレスと時刻による手作業照合しかできない。ユーザー影響は間接的（サポート対応不能リスク）。</risk>
</finding>

<finding id="M-5" severity="medium" title="サーバー側に決済チェックが皆無 — API直叩きで未払い生成が可能">
<known-fact>
- api/sessions.ts / api/answers.ts / api/generate.ts のいずれも支払状態を確認しない。決済ゲートは App.js:974 の `if (showLanding && !hasPaid)` というフロントの state のみ。
- POST /api/sessions → POST /api/answers ×16 → POST /api/generate を直接呼べば、Stripe を経由せず結果（Gemini 呼び出しコスト込み）を取得できる。
</known-fact>
<risk>一般ユーザーの正規フローでは顕在化しないが、「未払いで進めてしまう経路」が API レベルで常時開放されている。スクリプト化されれば Gemini コストの流出と商品価値の毀損になる。</risk>
</finding>

### 影響度: 低

<finding id="L-1" severity="low" title="既知バグ: ?preview=result が機能せずランディングページが表示される（観点6・確認済み）">
<known-fact>
- App.js:25 で PREVIEW_MODE を取得。App.js:675-682 の useEffect は setShowSplash(false) / setShowNameInput(false) / setUserName('John') / setResult(MOCK_RESULT) を行うが、**setShowLanding(false) を行わない**（hasPaid も false のまま）。
- レンダリング分岐はランディング判定 `if (showLanding && !hasPaid)`（App.js:974）が結果表示判定 `if (result)`（App.js:1155）より先に評価される。showLanding の初期値は true（App.js:656）のため、プレビューモードでも常にランディングページで止まる。
- 補足: タスク指示にあった「940行目/1121行目付近」は現行ファイルでは 974 行 / 1155 行に相当（直近の React.lazy 化コミットで行番号がシフト。940 行付近は現在 Ambassador 分岐）。
</known-fact>
<risk>影響は開発・デザイン確認作業のみ（一般ユーザーには無影響）。</risk>

修正方針案（修正は未実施）:
1. **案A（最小差分）**: App.js:675-682 の useEffect に `setShowLanding(false);` を1行追加。分岐条件が `showLanding && !hasPaid` のため hasPaid の変更は不要。難点: 初回レンダー時にスプラッシュ/ランディングが1フレーム見える可能性。
2. **案B（推奨・フラッシュなし）**: state 初期値で分岐する。`const isPreview = PREVIEW_MODE === 'result';` を用い `useState(!isPreview)` を showSplash（App.js:655）と showLanding（App.js:656）に適用し、useEffect には setResult / setUserName のみ残す。effect の実行順序に依存しない。
3. **案C（分岐順の変更）**: `if (result)` ブロックをランディング分岐より前へ移動。通常フローで result と showLanding が同時に真になる経路は存在しないため理論上安全だが、暗黙の画面優先順位を変えるため回帰確認の範囲が広い。
</finding>

<finding id="L-2" severity="low" title="生成の並行二重実行ガードなし（二重Gemini呼び出し・結果二重保存の可能性）">
<known-fact>GenerationService.ts:35-37 の既生成チェックは `session.completed && session.result_kanji_name` だが、completed は生成成功後にのみ set（:74）。並行 POST は両方チェックを通過し、Gemini 2回呼び出し + naming_results 2行 INSERT（:180-217、getResult は最新1行を返すため表示は正常）。</known-fact>
<risk>フロントは生成を1回しか呼ばないため通常は発生しない。二重「課金」には直結しない（決済は生成と独立のため）。コスト面の軽微なリスク。[推測]: リロード後の再決済→再生成の場合はセッションが別なので競合しない。</risk>
</finding>

<finding id="L-3" severity="low" title="マッチングスコアが定数（演出）">
<known-fact>AIKanjiGenerationService.ts:464-475 で gender_match=90 / motivation_match=95 の固定値。total は 0.3/0.7 加重（:432）で常に 94 前後。結果画面のスコアバー（App.js:590-597）は実質的に固定の演出。</known-fact>
<risk>バグではなく仕様確認事項。「診断スコア」として表示する以上、景品表示上の観点は事業判断が必要。</risk>
</finding>

<finding id="L-4" severity="low" title="デモコードは検証時点で消費される">
<known-fact>api/demo/validate.js:64-72 検証成功と同時に used_count をインクリメント（原子的更新自体は正しく TOCTOU 対策済み）。PaymentModal.js:178-180 は 1.5 秒後に onSuccess。またランディングでは sessionId が undefined のため demo_code_usage.session_id は常に NULL（validate.js:35, PaymentModal.js:292）。</known-fact>
<risk>適用直後に離脱すると1回分が消費される。max_uses の小さいコードで「使えるはずなのに使えない」問い合わせの原因になりうる。</risk>
</finding>

<finding id="L-5" severity="low" title="calligrapher-request のDB保存失敗を握りつぶして成功応答">
<known-fact>api/calligrapher-request.js:66-80 INSERT 失敗を catch してログのみで続行（コメント「Table might not exist」）。メールは送信されるため、ユーザーには成功に見えるが管理画面の追跡（calligraphy_requests）から漏れる可能性がある。migrations/003 でテーブルは定義済みのため通常は発生しない。</known-fact>
<risk>書道納品の管理台帳とメール実績がズレるリスク。ユーザー影響は間接的。</risk>
</finding>

<finding id="L-6" severity="low" title="致命的エラーの文言3種が未翻訳・エラー画面に再開手段なし">
<known-fact>'Failed to initialize session'（App.js:773）、'Session not ready. Please try again.'（App.js:852）、'Failed to generate result'（App.js:880）は locales を経由しない英語ハードコード。表示先の App.js:1150-1152 は `<div className="error">` のみで、リトライボタンも言語セレクタもない。</known-fact>
<risk>H-3/H-5 の付随問題だが、独立した i18n 欠陥として全11言語ユーザーに影響。</risk>
</finding>

<finding id="L-7" severity="low" title="Stripe公開鍵未設定ビルドでは決済UIが無言で消える">
<known-fact>PaymentModal.js:15-16, 228-230 REACT_APP_STRIPE_PUBLISHABLE_KEY が無いと stripePromise=null → コンポーネントは null を返す。REACT_APP_SKIP_PAYMENT が 'true' でなければ（App.js:749, 1059-1073）ランディングに決済手段が一切表示されず、先に進む方法がなくなる。</known-fact>
<unconfirmed>本番ビルドの環境変数は未確認。現在の本番で発生しているかは不明（ビルド設定ミス時にのみ顕在化する地雷）。</unconfirmed>
</finding>

---

## 2. 「問題なし」を確認できた観点

<known-fact>
1. **UI翻訳キーの網羅性（locales/index.js）**: 11言語 × 62キーを node で機械照合し欠落ゼロ。App.js / PaymentModal.js / LanguageSelector.js で使用される t() キー41件（grep 抽出、正規表現誤検出3件を除外）はすべて en に定義があり、useTranslation.js:21-28 の en フォールバックも正しく機能する。動的参照の loading1〜loading4 / back / scrollHintCalligraphy 等も定義確認済み。※ ただし PaymentModal はそもそも t() を使っていない（M-3）。
2. **questions.json の整合性（pt を除く）**: 残り10言語は全17問・全選択肢で翻訳が存在。next 参照は全て有効（壊れ参照ゼロ・ループなし）。全経路が Q0 含め17問 = Q1〜Q16 の16回答で、GenerationService.ts:43 の「answers.length >= 16」要件と一致（Q0 を送信しない App.js:826 の仕様と辻褄が合っている）。進捗表示の parseInt（App.js:816）も全IDが Q+数字形式のため NaN にならない。
3. **Stripe webhook の冪等性・安全性**: 署名検証あり（webhook.js:100）。payments の upsert は migrations/004_partner_system.sql:33 の UNIQUE 制約と整合し、`ON CONFLICT ... WHERE status != 'succeeded'` + `(xmax=0)` 判定（webhook.js:119-144）で重複 webhook でもレコード重複・ロイヤリティ二重計上は発生しない。migrations/008 の部分ユニークインデックスは冗長だが無害。
4. **金額の改ざん耐性**: 価格はサーバー側で partners テーブルから決定（create-payment-intent.js:36-52）。クライアントから金額を指定する経路はない。
5. **CORS**: 本番オリジン（app.kanjiname.jp / kanjiname.jp）は whitelist 済み（api/lib/security.js:9-18）。同一オリジン構成（/api rewrite）のため本線フローで CORS 起因の失敗経路はない。
6. **メールテンプレートの言語網羅**: CALLIGRAPHY_EMAIL_CONFIG / KANJI_NAME_EMAIL_CONFIG とも pt を含む11言語完備（api/lib/email.js を node で確認）。
7. **回答送信の順序保証**: 最終質問時に pendingSubmitsRef の全 Promise を await してから生成を呼ぶ（App.js:867-877）ため、ネットワーク遅延による「生成が回答を追い越す」競合はフロント起因では発生しない（HTTPエラーの無検知は H-4 の通り別問題）。
</known-fact>

---

## 3. 調査できなかった範囲とその理由

<unconfirmed>
1. **本番 Vercel の実行時間上限**（プラン種別・Fluid Compute の有無）: 環境情報にアクセスしないため。H-5 の顕在化条件（generate の 504）は実測未確認。
2. **本番環境変数**（GEMINI_API_KEY / STRIPE 鍵 / REACT_APP_SKIP_PAYMENT / REACT_APP_STRIPE_PUBLISHABLE_KEY / ADMIN_EMAIL）: 同上。L-7 は地雷の指摘であり現況は不明。
3. **本番DBの実スキーマ**: schema.sql / migrations が本番に全て適用済みかは接続禁止のため未確認。本報告のDB根拠はすべてリポジトリ内定義。
4. **Stripe / Gemini / Resend の実応答・レイテンシ・エラー率**: 外部接続禁止のため。AI応答パース（AIKanjiGenerationService.parseResponse）の実データでの成功率も未検証。
5. **frontend/api/ ディレクトリと src/routes/・src/server.ts（Express 旧構成）**: ルート vercel.json の構成から非デプロイ対象と判断し、詳細調査対象から除外。デプロイ実体（Vercel ダッシュボード）は未確認。
6. **本線フロー外の画面**（Admin / Partner / Ambassador / TankaPrompt / StoreLanding / Terms / Privacy）: タスクスコープ外。
7. **実ブラウザでの再現テスト**: 読み取り専用・机上調査の制約により、全指摘はコード読解と機械照合に基づく（各所に [事実]/[推測] を明記）。
</unconfirmed>

---

## 4. 修正優先度の提言（参考）

決済済みユーザーを直撃する順に:
1. H-1（pt 空白画面）— pt を questions.json に追加するか、当面 SUPPORTED_LANGUAGES から pt を外す即応策の二択。
2. H-4 + H-3（response.ok 未検査系）— ApiClient 全メソッドに ok 検査と例外化を入れるだけで、白画面と無音回答欠落の大半が「エラー表示+再試行可能」に変わる。
3. H-2 + H-5（永続化とリカバリ）— sessionId / hasPaid / paymentIntentId の sessionStorage 保存と、既存の `GET /api/generate?action=result` を使った復元で二重課金経路を塞ぐ。
4. M-2（lang 不達）— generate.ts:35 に lang を渡す1行 + フロント参照キーの統一（explanation.user）。
5. M-1（回答修正不能）— AnswerService の upsert 化（ON CONFLICT DO UPDATE）。

*報告者: researcher（読み取り専用調査）。本ファイルの新規作成以外、リポジトリへの変更は行っていない。*
