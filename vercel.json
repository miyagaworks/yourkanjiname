{
  "buildCommand": "npm run build && cd frontend && npm install && REACT_APP_API_URL=/api npm run build && cp -r build/* ../public/ && node scripts/generate-app-index.js",
  "redirects": [
    {
      "source": "/:path*",
      "has": [
        {
          "type": "host",
          "value": "www.kanjiname.jp"
        }
      ],
      "destination": "https://kanjiname.jp/:path*",
      "permanent": true
    }
  ],
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api/:path*"
    },
    {
      "source": "/((?!api/).*)",
      "destination": "/index.html"
    }
  ],
  "crons": [
    {
      "path": "/api/cron/save-exchange-rate",
      "schedule": "0 23 28-31 * *"
    }
  ]
}
